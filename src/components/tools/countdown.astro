---
// 日期倒计时：到目标日期的剩余天/时/分/秒
---

<div>
  <div class="field-label"><span>目标日期时间</span></div>
  <input id="cd-in" type="datetime-local" />
</div>
<div id="cd-out" class="cd-out">请选择目标时间</div>
<div class="btn-row">
  <button id="cd-toggle" class="primary">开始</button>
  <button id="cd-reset">重置</button>
</div>

<style>
  .cd-out { font-size: 22px; font-weight: 600; margin: 14px 0; font-variant-numeric: tabular-nums; }
  .cd-out span { color: var(--brand); }
</style>

<script>
  const $ = (id) => document.getElementById(id);
  const input = $('cd-in'), out = $('cd-out'), toggle = $('cd-toggle');
  let timer = null;

  function fmt(n) { return String(n).padStart(2, '0'); }
  function tick() {
    const target = new Date(input.value).getTime();
    if (isNaN(target)) { out.textContent = '请选择有效时间'; return; }
    const diff = target - Date.now();
    if (diff <= 0) { stop(); out.innerHTML = '⏰ 时间到！'; return; }
    const d = Math.floor(diff / 86400000);
    const h = Math.floor((diff % 86400000) / 3600000);
    const m = Math.floor((diff % 3600000) / 60000);
    const s = Math.floor((diff % 60000) / 1000);
    out.innerHTML = `<span>${d}</span> 天 <span>${fmt(h)}</span>:<span>${fmt(m)}</span>:<span>${fmt(s)}</span>`;
  }
  function start() { if (timer || !input.value) return; toggle.textContent = '暂停'; tick(); timer = setInterval(tick, 1000); }
  function stop() { if (timer) { clearInterval(timer); timer = null; } toggle.textContent = '开始'; }

  toggle.addEventListener('click', () => { timer ? stop() : start(); });
  $('cd-reset').addEventListener('click', () => { stop(); out.textContent = '请选择目标时间'; });
</script>
